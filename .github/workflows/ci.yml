name: SkyCMS Backend CI Pipeline (GHCR)

on:
  push:
    tags: [ "v*.*.*" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/skylab-kulubu/cms-backend:latest
            ghcr.io/skylab-kulubu/cms-backend:${{ github.sha }}
            ghcr.io/skylab-kulubu/cms-backend:${{ github.ref_name }}
